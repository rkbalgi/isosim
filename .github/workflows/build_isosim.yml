name: build
on:
  push:   
    branches: 
      - master
  schedule:
  - cron: '0 0 */7 * *'

jobs:
  checks:
    name: run
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master

    - name: run
      uses: cedrickring/golang-action@1.5.1
